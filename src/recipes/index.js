// NOTE: Make sure that the file name and the slug of an article
// are the same, as the file name is treated as the slug in the
// build process.

export default [
  require('./kohlrabi-lasagne.js').default,
  require('./lentils-wrap.js').default,
  require('./porcini-risotto.js').default,
];
